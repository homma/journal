-----
### Cpp2 で Raylib を使用する
> date: 2025-10-04  
> topics: c++, cpp2, raylib

#### About

Cpp2 から Raylib を使用してみます。

#### Code

main.cpp2
````cpp2
#include <raylib.h>

white: Color = (255, 255, 255, 255);
black: Color = (0, 0, 0, 255);

update: () = {

  BeginDrawing();

  ClearBackground(white);

  {
    text := "This is a raylib window";
    x := 110;
    y := 170;
    h := 30;
    color := black;

    DrawText(text, x, y, h, color);
  }

  EndDrawing();
}

main: () = {
  w := 600;
  h := 400;
  title := "My Raylib Window";

  InitWindow(w, h, title);

  while !WindowShouldClose() {
    update();
  }

  CloseWindow();
}
````

build.sh
````sh
#!/bin/sh -x

PATH=./cppfront/source:${PATH}
CPPFRONT_INCLUDE=./cppfront/include
RAYLIB_INCLUDE=$(brew --prefix raylib)/include
RAYLIB_LIB=$(brew --prefix raylib)/lib
FILENAME=$(echo ${1} | cut -f 1 -d .)

cppfront ${1} -cl -in -o ${FILENAME}.cpp
c++ ${FILENAME}.cpp -std=c++2c -I${RAYLIB_INCLUDE} -I${CPPFRONT_INCLUDE} -L${RAYLIB_LIB} -lraylib -o ${FILENAME}
````

#### ビルドと実行

````console
$ vi main.cpp2
$ vi build.sh
$ chmod +x ./build.sh
$ ./build.sh main.cpp2
$ ./main
````

-----
### Cpp2 で SDL3 を使用する
> date: 2025-10-04  
> topics: c++, cpp2, sdl3

#### About

Cpp2 から SDL3 を使用してみます。

#### Code

main.cpp2
````cpp2
#include <SDL3/SDL.h>

main: () = {
  v := SDL_GetVersion();
  std::println("{}.{}.{}",
               SDL_VERSIONNUM_MAJOR(v),
               SDL_VERSIONNUM_MINOR(v),
               SDL_VERSIONNUM_MICRO(v));
}
````

build.sh
````sh
#!/bin/sh -x

PATH=./cppfront/source:${PATH}
CPPFRONT_INCLUDE=./cppfront/include
SDL3_INCLUDE=$(brew --prefix SDL3)/include
SDL3_LIB=$(brew --prefix SDL3)/lib
FILENAME=$(echo ${1} | cut -f 1 -d .)

cppfront ${1} -cl -in -o ${FILENAME}.cpp
c++ ${FILENAME}.cpp -std=c++2c -I${SDL3_INCLUDE} -I${CPPFRONT_INCLUDE} -L${SDL3_LIB} -lSDL3 -o ${FILENAME}
````

#### ビルドと実行

````console
$ vi main.cpp2
$ vi build.sh
$ chmod +x ./build.sh
$ ./build.sh main.cpp2
$ ./main
3.2.22
````

-----
### Cpp2 を試す
> date: 2025-10-04  
> topics: c++, cpp2

#### About

Cpp2 をインストールし、実際に使ってみます。

#### インストール

````console
$ git clone --depth 1 https://github.com/hsutter/cppfront.git
$ cd cppfront/source
$ c++ cppfront.cpp -std=c++20 -o cppfront
$ ./cppfront -vers

cppfront compiler v0.8.2 Build A828:0952
SPDX-License-Identifier  Apache-2.0 WITH LLVM-exception
Copyright (c) 2022-2025  Herb Sutter
````

#### Hello, World

hello.cpp2
````cpp2
main: () = {
  std::println("Hello, World");
}
````

build.sh
````sh
#!/bin/sh -x

PATH=./cppfront/source:${PATH}
CPPFRONT_INCLUDE=./cppfront/include
FILENAME=$(echo ${1} | cut -f 1 -d .)

cppfront ${1} -cl -in -p -o ${FILENAME}.cpp
c++ ${FILENAME}.cpp -std=c++2c -I${CPPFRONT_INCLUDE} -o ${FILENAME}
````

ビルドと実行
````console
$ vi hello.cpp2
$ vi build.sh
$ chmod +x ./build.sh
$ ./build.sh hello.cpp2
$ ./hello 
Hello, World
````

#### cppfront コマンドのオプション

- `-vers` : バージョンを出力する
- `-help` : ヘルプを出力する
- `-p` : Cpp2 の文法のみを受け入れる（C++ の文法と混ぜられない）
- `-in` : `std` ヘッダーを全てインクルードする
- `-cl` : メタ情報を出力しない
- `-o <filename>` : 出力先のファイル名を指定

#### メモ
- `Apple clang version 17.0.0 (clang-1700.3.19.1)` では C++ の `import` が使えません
- 代わりに `-in` オプションで `std` ヘッダーをインクルードします

- Neovim では `.cpp2` ファイルのシンタックスハイライトが効きません
- GitHub も `cpp2` のシンタックスハイライトはできなさそう
- Cpp2 のフォーマッターは標準装備されていなさそう

#### References
- https://github.com/hsutter/cppfront
- https://hsutter.github.io/cppfront/
