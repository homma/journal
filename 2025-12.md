-----
### Raylib のネイティブウィンドウ情報にアクセスする
> date: 2025-12-29  
> topics: objective-c++, raylib, cocoa

#### About

Raylib はデフォルトでは GLFW を使用して OS ネイティブのウィンドウを作成します。  
`GetWindowHandle` 関数を使用してネイティブウィンドウの情報にアクセスします。  

#### main.mm

````cpp
#import <Cocoa/Cocoa.h>
#include <raylib.h>

auto update() -> void {
  BeginDrawing();

  ClearBackground(WHITE);

  EndDrawing();
}

auto main() -> int {
  SetTargetFPS(60);
  InitWindow(600, 400, "Cocoa Test");

  auto window = (NSWindow *)GetWindowHandle();
  auto cview = [window contentView];

  NSLog(@"window: %@", window);
  NSLog(@"content view: %@", cview);

  while (!WindowShouldClose()) {
    update();
  }

  CloseWindow();
}
````

#### build.sh

````sh
#!/bin/sh -x

RAYLIB_INC="-I$(brew --prefix raylib)/include"
RAYLIB_LIB="-L$(brew --prefix raylib)/lib"
RAYLIB_OPT="${RAYLIB_INC} ${RAYLIB_LIB} -lraylib"

COCOA_OPT="-framework Cocoa"
CXX_OPT="-ObjC++ --std=c++2b"

c++ main.mm -o test_cocoa ${CXX_OPT} ${RAYLIB_OPT} ${COCOA_OPT}
````
