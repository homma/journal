-----

### Rust から SDL2 を使用する
> date: 2024-12-13  
> topics: rust, sdl2

Rust から SDL2 を使用する方法は 3 種類あると思います。

1. sdl2 crate を使用する
2. sdl2-sys create を使用する
3. bindgen を使用する

今回は 2 番目の方法で Rust から SDL2 を呼び出す方法です。

#### 新規プロジェクトの作成

````sh
$ cargo new sdl_test
$ cd sdl_test
````

#### sdl2-sys crate の追加

`sdl2-sys` crate を追加するため `cargo add` コマンドを実行し、`Cargo.toml` ファイルを編集します。  

````sh
$ cargo add sdl2-sys
$ vi Cargo.toml
````

SDL2 ライブラリの検索に `pkg-config` コマンドを使用するため、features に追加しています。  
SDL2 への binding を再生成するため、bindgen を features に追加しています。

````toml
# Cargo.toml

[dependencies]
sdl2-sys = { version = "0.37.0", features = ["use-pkgconfig", "bindgen"] }
````

#### `main.rs` の作成

````sh
$ vi src/main.rs
````

`SDL_GetVersion` 関数を呼び出してバージョンを出力するだけの簡単なプログラムです。  

````rust
// main.rs

fn main() {
    test();
}

fn test() {
    let major = 0;
    let minor = 0;
    let patch = 0;
    let mut ver = sdl2_sys::SDL_version {
        major,
        minor,
        patch,
    };
    unsafe { sdl2_sys::SDL_GetVersion(&mut ver) };
    println!("{}.{}.{}", ver.major, ver.minor, ver.patch);
}
````

#### ビルドと実行

````sh
$ cargo check
$ cargo fmt
$ cargo run
````
