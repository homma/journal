-----

### Rust から SDL2 を使用する
> date: 2024-12-13  
> topics: rust, sdl2

Rust から SDL2 を使用する方法は 3 種類あると思います。

1. sdl2 crate を使用する
2. sdl2-sys create を使用する
3. bindgen を使用する

今回は 2 番目の方法で Rust から SDL2 を呼び出す方法です。

#### 環境の準備

````sh
$ cargo new sdl_test
$ cd sdl_test
$ cargo add sdl2-sys
$ vi src/main.rs
````

#### `main.rs` の作成

````rust
fn main() {
    test();
}

fn test() {
    let major = 0;
    let minor = 0;
    let patch = 0;
    let mut ver = sdl2_sys::SDL_version {
        major,
        minor,
        patch,
    };
    unsafe { sdl2_sys::SDL_GetVersion(&mut ver) };
    println!("{}.{}.{}", ver.major, ver.minor, ver.patch);
}
````

#### ビルドの設定

````sh
$ mkdir .cargo
$ echo .cargo/ >> .gitignore
$ vi .cargo/config.toml
[build]
rustflags = ["-L/path_to/sdl2/lib"]
````

#### ビルドと実行

````sh
$ cargo check
$ cargo fmt
$ cargo run
````
