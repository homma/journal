-----
### SDL3 をカレントディレクトリに配置して使用する
> date: 2025-05-01
> topics: c++, sdl3

#### About

SDL3 をカレントディレクトリにダウンロードしてプログラムをビルドし、実行します。  

macOS では、SDL3 は Framework として配布されています。  
通常は ~/Library/Frameworks にコピーすることが想定されているようです。  
今回はカレントディレクトリに配置した状態で使用します。  

#### SDL3 のインストール

GitHub のリリースからダウンロードし、必要なファイルをコピーします。  

````console
$ curl -# -O -L https://github.com/libsdl-org/SDL/releases/download/release-3.2.10/SDL3-3.2.10.dmg
$ DMG=SDL3-3.2.10.dmg
$ hdiutil attach ${DMG}
$ cp -Rp /Volumes/SDL3/SDL3.xcframework/macos-arm64_x86_64/SDL3.framework .
$ hdiutil detach /Volumes/SDL3
$ rm ${DMG}
````

#### main.cpp

````cpp
#include <SDL3/SDL.h>
#include <print>

auto main() -> int {
  auto v = SDL_GetVersion();
  std::println("{}.{}.{}", SDL_VERSIONNUM_MAJOR(v), SDL_VERSIONNUM_MINOR(v),
               SDL_VERSIONNUM_MICRO(v));
}
````

#### SConstruct

実行時にカレントディレクトリの Framework を参照させるため、`rpath` オプションを追加しています。  

````python
import subprocess

# Executable
target_name = "sdl_test"
src = "main.cpp"

# Library
## SDL3
sdl3 = {}
sdl3["framework"] = ["SDL3"]
sdl3["framework_path"] = ["."]
sdl3["link_flag"] = ["-Wl,-rpath,."]

Program(
    target=target_name,
    source=src,
    FRAMEWORKS=sdl3["framework"],
    FRAMEWORKPATH=sdl3["framework_path"],
    CCFLAGS=["-std=c++2c"],
    LINKFLAGS=sdl3["link_flag"],
)
````

#### ビルド

````console
$ scons
````

#### 実行

````console
$ ./sdl_test
3.2.10
````
