-----

### Panda3D を C++ プロジェクト用にインストールする
> date: 2025-03-15
> topics: c++, panda3d

以下の手順ではグローバルにインストールすることを回避するため、手動でパッケージの内容を抽出している。  

ただし、この方法では x86_64 のライブラリになってしまう。  
そのため、arm64 のライブラリは Python のパッケージからコピーしている。  
もう少し調査が必要。  

mpkg ファイルからグローバルインストールすれば良いのかもしれない。  

#### C++ 用のライブラリとヘッダーファイルのインストール

最新版の SDK の URL を確認

````console
$ open https://www.panda3d.org/download/latest
````

SDK のダウンロード。  
`base.pkg` にライブラリが入っている。  
`headers.pkg` にヘッダーファイルが含まれている。  

````console
$ SDK_URL=https://www.panda3d.org/download/panda3d-1.10.15/Panda3D-SDK-1.10.15-MacOSX10.9.dmg
$ P3_DMG=Panda3D-SDK-1.10.15-MacOSX10.9.dmg
$ curl -# -L -O ${SDK_URL}
$ hdiutil attach ${P3_DMG}
$ cp /Volumes/Panda3D\ SDK\ 1.10.15/Panda3D.mpkg/Contents/Packages/base.pkg .
$ cp /Volumes/Panda3D\ SDK\ 1.10.15/Panda3D.mpkg/Contents/Packages/headers.pkg .
$ hdiutil info
## change disk4 to the attached volume
$ hdiutil detach /dev/disk4
$ rm ${P3_DMG}
````

ライブラリとヘッダーファイルの抽出

````console
$ pkgutil --expand-full base.pkg out.base
$ pkgutil --expand-full headers.pkg out.headers
$ mv out.base/Payload/Library/Developer/Panda3D .
$ mv out.headers/Payload/Library/Developer/Panda3D/include Panda3D/
$ rm -rf out.*
$ rm *.pkg
````

#### arm64 のライブラリをコピーする

ここまでの手順では、x86_64 用のライブラリがインストールされてしまう。  
そのため、arm64 用のライブラリを Python のパッケージからコピーする。  

まだパッケージのインストールを実施していない場合は pip でインストールする。  

````console
$ pip3 install panda3d
````

arm64 用のライブラリをコピーする。  

````console
$ mv Panda3D/lib Panda3D/lib_x86
$ mkdir Panda3D/lib
$ PY_DIR=$(python3 -c 'import site;print(site.USER_SITE)')
$ cp -Rp ${PY_DIR}/panda3d/lib*.dylib Panda3D/lib
$ cp -Rp ${PY_DIR}/panda3d_tools/libp3framework.1.10.dylib Panda3D/lib
````

必要なライブラリにリンクを張る。  

````
$ cd Panda3D/lib
$ VER=1.10; for i in panda pandafx pandaexpress p3dtoolconfig p3dtool p3direct p3framework; do ln -s lib${i}.${VER}.dylib lib${i}.dylib; done
````

#### C++ プログラムのコンパイル

````console
$ vi myapp.cxx
````

````console
$ g++ myapp.cxx -o myapp -std=c++11 -IPanda3D/include -LPanda3D/lib -lpanda -lpandafx -lpandaexpress -lp3dtoolconfig -lp3dtool -lp3direct -lp3framework
````

#### C++ プログラムの実行

````console
$ DYLD_LIBRARY_PATH=Panda3D/lib ./myapp
````

-----

### Panda3D をインストールする
> date: 2025-03-15
> topics: python, panda3d

#### Python 用パッケージのインストール

````console
$ pip3 install panda3d
````

-----

### pip を使用できるように設定する
> date: 2025-03-15
> topics: python

Homebrew で Python を使用していると pip3 コマンドの実行時に警告が表示されます。  
パッケージをユーザーインストールする分には問題は発生しないため、警告が表示されないように設定します。  

#### 設定手順

````console
$ mkdir -p ~/.config/pip
$ touch ~/.config/pip/pip.conf
````

#### 設定内容

````ini
# ~/.config/pip/pip.conf

[global]
break-system-packages = true
user = true
````

#### テスト

````console
$ pip3 install pycowsay
$ type pycowsay
$ pip3 uninstall -y pycowsay
````

